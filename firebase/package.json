{
  "name": "firebase",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase": "^9.15.0"
  }
}